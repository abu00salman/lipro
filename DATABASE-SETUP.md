# نشر قاعدة البيانات — دليل خطوة بخطوة

هذا الدليل ينشر خادم التسجيل الحقيقي (حفظ البيانات + التعرّف على العائدين)
**بدون سطر أوامر إطلاقًا** — كل شيء من متصفحك، بنفس أسلوب رفع الموقع على GitHub.

الوقت المتوقع: **١٥ دقيقة**.

---

## قبل ما تبدأ

بما أنك اشتريت النطاق من Cloudflare، عندك حساب أصلًا. افتح:

**dash.cloudflare.com**

---

## الخطوة ١ — إنشاء قاعدة البيانات

1. من القائمة الجانبية اضغط **Workers & Pages**
2. اضغط تبويب **D1 SQL Database** (بجانب Overview)
3. اضغط **Create Database**
4. الاسم: `lipro-db`
5. اضغط **Create**

بعد الإنشاء تفتح صفحة القاعدة تلقائيًا — **لا تغلقها**، ستحتاجها في الخطوة التالية.

---

## الخطوة ٢ — إنشاء الجداول

1. في نفس صفحة `lipro-db`، اضغط تبويب **Console**
2. افتح ملف `backend/schema.sql` (المرفق في نفس المجلد) بأي محرر نصوص، أو اضغط عليه هنا في المحادثة لعرضه
3. انسخ **المحتوى كاملًا**
4. الصقه في مربع الاستعلام في صفحة الـ Console
5. اضغط **Execute**

لازم تشوف رسالة نجاح خضراء، وتظهر جداول جديدة في القائمة الجانبية:
`registrations` و `rate_limit`.

> ✅ **للتأكد:** اكتب في مربع الاستعلام `SELECT name FROM sqlite_master WHERE type='table';`
> واضغط Execute — لازم تشوف الجدولين.

---

## الخطوة ٣ — إنشاء الخادم (Worker)

1. ارجع لصفحة **Workers & Pages**
2. اضغط **Create**
3. اختر **Workers** ← **Create Worker**
4. الاسم: `lipro-backend` (أو أي اسم تحبه — يظهر في الرابط لاحقًا)
5. اضغط **Deploy** (سينشر نسخة تجريبية افتراضية أول مرة — طبيعي)

---

## الخطوة ٤ — لصق الكود الحقيقي

1. بعد النشر، اضغط **Edit code**
2. تفتح محرر الأكواد — احذف كل المحتوى الموجود بالافتراضي
3. افتح ملف `backend/worker-bundle.js` (المرفق) وانسخ **محتواه كاملًا**
4. الصقه في المحرر
5. اضغط **Deploy** أعلى يمين المحرر

---

## الخطوة ٥ — ربط قاعدة البيانات بالخادم

1. من صفحة الـ Worker، اضغط تبويب **Settings**
2. اضغط **Bindings** من القائمة الجانبية داخل الإعدادات
3. اضغط **Add binding** ← اختر **D1 database**
4. **Variable name**: اكتب بالضبط `DB` (حرفان كبيران — الكود يبحث عن هذا الاسم تحديدًا)
5. **D1 database**: اختر `lipro-db` من القائمة
6. اضغط **Save**

---

## الخطوة ٦ — إضافة الإعدادات

في نفس صفحة **Settings ← Variables and Secrets**:

### متغيّر عادي (Text)
| الاسم | القيمة |
|---|---|
| `ALLOWED_ORIGINS` | `https://liproacademy.com,https://www.liproacademy.com` |

### سرّ محمي (Secret) — لصفحة الإدارة
| الاسم | القيمة |
|---|---|
| `ADMIN_KEY` | `9y9KsloGzXIsFj8GksjuEKTOZFcg` |

> 🔑 **احتفظ بهذا المفتاح في مكان آمن** — هو كلمة مرور صفحة عرض كل التسجيلات.
> غيّره لاحقًا من نفس الإعدادات وقتما تريد.

اضغط **Save and Deploy** بعد إضافة الاثنين.

---

## الخطوة ٧ — نسخ رابط الخادم

في أعلى صفحة الـ Worker تحت اسمه، تجد رابطًا بصيغة:

```
https://lipro-backend.YOUR-SUBDOMAIN.workers.dev
```

**انسخ هذا الرابط بالضبط** — ستحتاجه في الخطوة التالية.

---

## الخطوة ٨ — ربط الموقع بالخادم

1. افتح ملف `assets/lipro-checkout.js` (حمّله من مستودع GitHub، أو استخدم النسخة في المحادثة)
2. ابحث عن أول سطر:
   ```js
   window.LIPRO_API = window.LIPRO_API || 'https://lipro-payments.YOUR-SUBDOMAIN.workers.dev';
   ```
3. استبدل الرابط برابطك الحقيقي من الخطوة ٧
4. احفظ الملف
5. ارفعه على GitHub بنفس طريقة رفع أي ملف (Add file ← Upload files ← Commit)

---

## الخطوة ٩ — التأكد من نجاح كل شيء

افتح هذه الروابط بالترتيب (استبدل الرابط برابطك):

**١. فحص الحياة:**
`https://lipro-backend.YOUR-SUBDOMAIN.workers.dev/api/health`
لازم تشوف: `{"ok":true,"time":"..."}`

**٢. الإعدادات:**
`https://lipro-backend.YOUR-SUBDOMAIN.workers.dev/api/config`
لازم تشوف قائمة البرامج الأربعة.

**٣. صفحة التسجيل الحقيقية:**
افتح `liproacademy.com/register/` — لازم يظهر صندوق **"سبق وسجّلت معنا؟"** أعلى الصفحة. ظهوره يعني أن الخادم يعمل ومتصل.

**٤. صفحة الإدارة:**
`https://lipro-backend.YOUR-SUBDOMAIN.workers.dev/api/admin?key=9y9KsloGzXIsFj8GksjuEKTOZFcg`
تعرض جدولًا بكل التسجيلات (فارغ حاليًا لحد ما يسجّل أحد).

---

## كيف تستخدمها يوميًا

### لمراجعة التسجيلات
احفظ رابط الإدارة (الخطوة ٩-٤) في المفضّلة على جوالك. افتحه أي وقت لترى كل من سجّل، بياناتهم، ووظائف الأهل — بلا أي أدوات إضافية.

### عندما يسجّل أحد الأهالي مجددًا
النظام يتعرّف عليه تلقائيًا برقم جواله ويعرض له "تجديد الاشتراك" مباشرة — لا شيء يتطلب تدخلًا منك.

---

## استكشاف الأعطال

| المشكلة | السبب المحتمل | الحل |
|---|---|---|
| صندوق "سبق وسجّلت" لا يظهر | الرابط في `lipro-checkout.js` خاطئ | تأكد من نسخه بالضبط من الخطوة ٧ |
| `/api/health` يعطي خطأ | الـ Worker لم يُنشر بنجاح | ارجع للخطوة ٤ وتأكد من الضغط على Deploy |
| صفحة الإدارة تقول "غير مصرّح" | المفتاح في الرابط غلط | تأكد من نسخ `ADMIN_KEY` بالضبط بلا مسافات |
| التسجيل يفشل برسالة خطأ عام | ربط قاعدة البيانات ناقص | تأكد أن اسم المتغيّر بالضبط `DB` في الخطوة ٥ |

---

## ملاحظة أمان

هذا الخادم **لا يحتوي أي بيانات بطاقات بنكية أو مفاتيح دفع** — فقط بيانات التسجيل (الاسم، الجوال، الوظائف، البرنامج). آمن للنشر الآن بغض النظر عن قرارك بخصوص ميسر أو أي بوابة دفع لاحقًا.

عندما تقرر تفعيل الدفع بالبطاقة مستقبلًا، الملفات في `backend/src/` (وليس `worker-bundle.js`) هي الجاهزة لذلك — أخبرني وقتها وأدمج الاثنين معًا بلا إعادة بناء من الصفر.
